# Changelog

## 0.1.1

- Explain Codex hook review and trust during installation and diagnosis, including
  re-review after command changes and starting a fresh session after approval.
- Report hooks as configured and show the commands actually saved for each agent
  in `pixelhof doctor`.
- Respect `CLAUDE_CONFIG_DIR` and `CODEX_HOME` when locating agent configuration.
- Keep the CLI and Claude Code plugin versions aligned, and verify version tags
  before publishing.
